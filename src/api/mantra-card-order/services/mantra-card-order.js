'use strict';

/**
 * mantra-card-order service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mantra-card-order.mantra-card-order');
