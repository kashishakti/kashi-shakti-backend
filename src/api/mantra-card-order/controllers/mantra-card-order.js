'use strict';

/**
 * mantra-card-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mantra-card-order.mantra-card-order');
